# Copy to .env and fill in (do NOT commit .env)
#
# 1. Firebase Console → Project settings → Service accounts
# 2. Generate new private key → save OUTSIDE this repo
# 3. Set absolute path below

GOOGLE_APPLICATION_CREDENTIALS=/Users/you/secrets/homieboy-service-account.json
